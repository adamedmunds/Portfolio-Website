import { Typography } from '@mui/material';
import { Fragment } from 'react';

export const ProfileEdit = () => {
  return (
    <Fragment>
      <Typography>Oh Hi</Typography>
      <Typography>There</Typography>
    </Fragment>
  );
};
